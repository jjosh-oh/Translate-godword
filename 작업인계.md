# 개발 작업 인계 — 다른 계정에서 이어서 하기

이 문서는 **개발을 이어받는 사람(또는 다른 Claude 계정)** 을 위한 것입니다.
프로그램을 설치해서 **운영**하는 방법은 [인수인계서.md](인수인계서.md)를 보십시오.

기준일: 2026-09-06

---

## 1. 폴더만 공유하면 될까 — 아닙니다

`C:\Claude` 폴더에는 코드와 git 기록이 다 들어 있지만, **폴더 밖에 있어서 따라가지
않는 것**이 넷 있습니다.

| 폴더 밖에 있는 것 | 어디에 | 어떻게 할 것인가 |
|---|---|---|
| **API 키** (`.env`) | `%LOCALAPPDATA%\Programs\LiveWord\.env` | git에 없음(`.gitignore`). **새 계정은 자기 키를 새로 발급**하는 게 안전 — [인수인계서 2장](인수인계서.md) |
| **Google 인증 파일** | `.env`의 `GOOGLE_APPLICATION_CREDENTIALS` 경로 | 위와 같음 |
| **대화 기록·메모리** | `~/.claude/projects/C--Claude/memory/` | **계정이 바뀌면 안 따라갑니다.** 대신 내용은 전부 `CLAUDE.md`와 이 문서에 옮겨 두었습니다 |
| **`배포빌드/ngrok.exe`** (31MB) | 폴더 안에 있지만 git에는 없음 | [ngrok.com/download](https://ngrok.com/download)에서 다시 받으면 됩니다 |

그리고 **폴더 통째로 공유는 비효율적**입니다. 4.5GB 중 4.1GB가 `오디오편집기/`
(별개 프로젝트의 설교 mp3 파일들)입니다. 실제 코드는 `.git` 3.2MB면 전부 들어갑니다.

**→ GitHub로 넘기는 것을 권합니다.**

---

## 2. 다른 계정에서 시작하는 순서

### 2-1. 코드 받기

```bash
git clone https://github.com/jjosh-oh/Translate-godword.git C:\Claude
cd C:\Claude
git branch -a          # 작업 브랜치가 보이는지 확인
```

> **작업 브랜치 두 개는 2026-09-06에 GitHub로 올렸습니다.** clone 하면 바로 보입니다.
>
> ```bash
> git checkout stt-chirp3          # 지금까지 작업한 것 전부 (권장)
> git checkout fix-subtitle-stall  # Chirp 3 실험을 뺀 안정판
> ```
>
> 저장소가 **공개(public)** 입니다. 올리기 전에 키가 섞였는지 검사했고 깨끗했습니다.
> 앞으로 커밋할 때도 `.env`와 `*.json`이 `.gitignore`에 있는지 확인하십시오.

### 2-2. 개발 환경

```bash
pip install -r 배포빌드/requirements.txt
pip install pyinstaller
```

추가로 **Python 3.13**, **[Inno Setup 6](https://jrsoftware.org/isdl.php)**,
그리고 `배포빌드/ngrok.exe`(직접 내려받기)가 필요합니다.

### 2-3. 키 넣기

프로그램을 한 번 실행하면 설정 화면이 나옵니다. [인수인계서 2장](인수인계서.md)대로
Anthropic · Google Cloud · ngrok 키를 넣으면 `.env`가 만들어집니다.

### 2-4. 잘 되는지 확인

```bash
python 개발도구/문장끊기_테스트.py      # 서버 없이, 비용 0. 5개 다 통과해야 정상
```

---

## 3. 지금 어디까지 왔나

`main`은 안정판(v1.1)이고, **작업 브랜치 두 개에 아직 배포하지 않은 커밋 8개**가 있습니다.

```
main
 └─ fix-subtitle-stall          자막 멈춤·비용·문장 끊기 (7 커밋)
     └─ stt-chirp3              + Chirp 3 엔진 선택 (11 커밋, fix-subtitle-stall 포함)
```

| 커밋 | 무엇을 고쳤나 |
|---|---|
| `e55cf56` | 스페인어 등에서 자막이 60~80초씩 멈추던 문제 |
| `3d70f89` | **번역 비용 폭증** — 설교 원고 캐시가 매번 무효화 ($31 → $3.5) |
| `c0c4fc0` | 문장 끊기 개선 — 음성활동 이벤트 + LocalAgreement + 시간 안전망 |
| `2b9264b` | 인수인계서 추가 |
| `ac76cab` | **Chirp 3 엔진 추가** — 선택형, 기본값은 기존 V1 |
| `cdfb5f5` | CLAUDE.md 함정 추가 |
| `2304eee` | 자막이 20초 뒤 통째로 다시 나오던 문제 |
| `4d9dc56` | 문장 끊기 조율 — 실제 설교 음성으로 재보고 맞춤 |

**이 PC에는 `stt-chirp3` 빌드가 설치돼 있습니다.** 엔진 기본값이 `v1`이라
동작은 기존과 같습니다. **버전은 아직 1.1이고 릴리스는 안 했습니다.**

> **2026-09-07 갱신** — 그 뒤 `main`에 v1.2가 병합됐고, 지금 이 PC에는
> 브랜치 `stt-gemini-live`의 **v1.3**이 설치돼 실행 중입니다(커밋 `fc56331`).
> v1.3은 음성인식 엔진 **Gemini 3.5 Transcribe Live**를 선택형으로 추가한 것이고
> 기본값은 여전히 `v1`입니다. 아래 4장 ①을 보십시오. **릴리스는 아직 안 했습니다.**

---

## 4. 아직 결정 안 된 것

### ① 음성인식 엔진 — chirp_3는 보류, **Gemini 3.5 Live를 선택형으로 넣음** (2026-09-07)

기본값은 계속 `v1`입니다. 운영자 화면에서 세 엔진 중에 고릅니다.
아래 chirp_3 실측(2026-09-06)은 그대로 유효하고, 여기에 Gemini를 덧붙입니다.

**같은 설교 2분(8/30 1부, 35:00~37:00)을 `Segmenter`까지 통과시켜 잰 것**

| | 번역에 넘어간 문장 | 첫 자막 | 평균 간격 | 최대 공백 |
|---|---|---|---|---|
| **V1 (기본값)** | 35개 | 3.11초 | 3.56초 | **6.78초** |
| **Gemini 3.5 Live** | 30개 | **1.77초** | 3.91초 | 7.71초 |

첫 자막이 1.3초 빠르고 숫자 인식이 낫습니다(`15장 1, 2, 3절` / V1은 `15장 123절`).
**최대 공백은 1초쯤 뒤집니다.** 어느 쪽이 예배에서 나은지는 리허설로 판단하십시오.

**찬양 구간에서는 자막이 하나도 안 나옵니다.** 같은 파일 6:00~8:00(찬송)에서
V1은 181자, chirp_3는 77자를 인식했는데 **Gemini는 0건**입니다. VAD가 노래를
발화로 보지 않습니다. `start/end_of_speech_sensitivity`를 최고로 올려도 같았습니다.
찬양 시간에는 통역하지 않기로 했으므로 그대로 두었습니다.

> **다시 손댈 때 알아야 할 것 둘 (둘 다 하루를 잡아먹을 수 있는 함정입니다)**
>
> 1. 실시간 텍스트는 `interim_input_transcription`으로 **따로** 옵니다.
>    구글 문서의 기본 예제(`input_transcription`)만 읽으면 2분에 **4번**밖에
>    안 오고 최대 공백이 86초가 됩니다. 처음에 이걸로 "쓸 수 없는 엔진"이라고
>    잘못 판단했습니다.
> 2. 그 `input_transcription`은 V1의 `is_final`과 **다릅니다.** 발화 단위가 아니라
>    **그 턴 전체를 다시** 보냅니다. `final`로 넘기면 이미 나간 자막 여러 줄이
>    통째로 다시 나옵니다. 그래서 쓰지 않고, 누적이 끊기면(새 턴) `speech_end`로
>    남은 것을 내보낸 뒤 `stream_start`로 문장 끊기를 새로 시작합니다.
>
> 확인 도구: `개발도구/자막_시간측정.py <pcm> ko-KR English gemini_live`
> (LiveWord 실행 중이어야 하고 번역이 실제로 일어나 몇십 센트 듭니다)

**요금** — Gemini 3.5 Transcribe Live는 분당 $0.009로 V1($0.024)·chirp_3($0.016)보다
쌉니다. `.env`의 `GEMINI_API_KEY`가 필요하고, 이 키는 Cloud 서비스 계정과 별개입니다
(설정 화면 4번 칸에서 넣을 수 있습니다).

<details>
<summary>chirp_3 실측 (2026-09-06, 그대로 유효)</summary>

**정확도만이 아니라 속도까지 좋아지기 전에는 chirp_3로 넘어가지 않습니다.**
아래 실측이 근거입니다. 같은 2분 음성으로 세 설정을 재봤습니다.

| | 자막 출력 | 평균 공백 | 최대 공백 | 첫 자막 |
|---|---|---|---|---|
| **V1 (계속 사용)** | **164회** | **0.7초** | **2.6초** | 0.8초 |
| chirp_3 | 26회 | 4.7초 | 9.7초 | 5.9초 |
| chirp_3 + `endpointing_sensitivity=SUPERSHORT` | 34회 | 3.6초 | 8.0초 | 2.0초 |

V1은 단어 단위로 계속 흘러나오고 chirp_3는 5초 덩어리로 옵니다.
**이 차이는 설정으로 못 좁힙니다.**

찬양 구간(8/30 예배 18:00)은 차이가 더 큽니다.

| | 자막 출력 | 평균 공백 |
|---|---|---|
| **V1** | **64회** | **1.3초** |
| chirp_3 | 8회 | 15.0초 |

**chirp_3는 노래를 거의 못 알아듣습니다.** 잡음제거가 반주와 함께 노래도 죽이는 것으로 보입니다.

> 다시 검토할 때 알아야 할 것: `endpointing_sensitivity`는 지정하지 않으면
> `STANDARD`로 돕니다. `SUPERSHORT`로 주면 첫 자막이 5.9→2.0초로 빨라지지만
> 2분에 오인식이 2건 늘었고(비유는→비율은, "다리" 삽입) 문장을 더 잘게 끊습니다.
> `chirp_2`는 us 리전에 없고, 단어 타임스탬프는 chirp_3 스트리밍에서 지원되지 않습니다.

<details>
<summary>이전 검토 내용 (참고)</summary>

실제 설교 녹음(8/30 주일 1부) 2분 구간 두 개로 잰 결과입니다.

| | V1 (latest_long) | chirp_3 (V2) |
|---|---|---|
| 지시어 "이"를 앞말에 붙임 | **10회** (4분 동안) | 0회 |
| 오인식 | 마지막 **데모**, **혼신**의 결단, **거실** 뿐이고, 15장 **123절** | 마지막 **대목**, **헌신**의 결단, **것일** 뿐이고, 15장 **1, 2, 3절** |
| 자막 최대 공백 | **6.7초** | 12.8초 |
| 자막 평균 간격 | **3.5초** | 5.0초 |

"이"를 앞말에 붙이면 **마침표까지 같이 사라져서** 자막이 늦어집니다.
정확도는 chirp_3, 반응 속도는 V1입니다.

**chirp_3의 지연은 코드로 더 못 줄입니다.** 구글이 중간 결과를 6초마다 주는 것이
바닥입니다(V1은 0.25초마다). `개발도구/중간결과_빈도측정.py`로 확인할 수 있습니다.

→ **예배 리허설에서 송출 화면을 보며 직접 판단해야 합니다.**

</details>

</details>

### ② chirp_3의 분당 요금 — 확인 완료 (2026-09-06)

**chirp_3가 오히려 3분의 1 쌉니다.** 요금 때문에 chirp_3를 망설일 이유는 없습니다.

요금 페이지 본문에는 chirp_3라는 이름이 안 나옵니다(그래서 지난번에 못 찾았습니다).
**Speech-to-Text V2는 모델과 무관하게 SKU 하나로 청구**되기 때문입니다.
구글 청구 카탈로그 API에서 Cloud Speech API의 SKU 32개를 전부 확인했고,
chirp 전용 SKU는 존재하지 않습니다.

| 무엇을 쓰나 | 청구 SKU | 분당 | 무료 구간 |
|---|---|---|---|
| **지금(V1 `latest_long`)** | `60AE-2FE3-C3D8` Audio Length Standard | **$0.024** | 월 60분 |
| **chirp_3(V2)** | `3099-B70F-0949` Cloud Speech-to-Text Recognition | **$0.016** | 없음 |
| (참고) V1 + 데이터 로깅 허용 | `67F5-A183-E319` Standard (Logged) | $0.016 | 월 60분 |

- 셋 다 `global` SKU라 **지역에 따른 차이가 없습니다.** chirp_3를 us 멀티리전으로 붙여도 같습니다.
- 코드의 `use_enhanced=True`는 **요금에 영향이 없습니다.** 구글 문서상 enhanced는
  `phone_call`·`video` 모델에만 적용되고 `latest_long`은 Standard로 청구됩니다.
- V1의 월 60분 무료가 V2에는 없지만, 분당 차이가 커서 결과는 그래도 chirp_3가 쌉니다.

**월 4회 예배 기준 실제 금액**

| 예배당 마이크 켜는 시간 | 지금(V1) | chirp_3 | 차이 |
|---|---|---|---|
| 60분 | $4.32 | $3.84 | −$0.48 |
| 90분 | $7.20 | $5.76 | −$1.44 |
| 120분 | $10.08 | $7.68 | −$2.40 |

어느 쪽이든 번역(Claude) 비용에 비하면 작습니다. **엔진 선택은 요금이 아니라
①번의 정확도·지연으로 판단하십시오.**

> 실제 청구액을 대조하려면 Google Cloud 콘솔 → 결제 → 비용 테이블에서 위 **SKU 이름**으로
> 거르면 됩니다. V1을 썼으면 `Audio Length Standard`, chirp_3를 썼으면
> `Cloud Speech-to-Text Recognition`으로 찍힙니다.

### ③ v1.3 정식 배포

버전 올리기와 빌드·설치·동작 확인은 **끝났습니다**(2026-09-07, `APP_VERSION`과
`AppVersion` 둘 다 `1.3`). 남은 것은 리허설과 릴리스뿐입니다.

1. ~~버전 두 곳 올리기~~ — 완료
2. ~~빌드 → 설치 → 실행 확인~~ — 완료. 남은 것은 **예배 리허설에서 엔진을
   `gemini_live`로 두고 송출 화면을 직접 보는 것**입니다. 여기서 판단이 서면
   `main`에 병합하십시오
3. ZIP은 **ZipCrypto** 방식 (윈도우 기본 압축 해제가 AES를 못 풂)
4. `gh release create`는 404가 납니다 — CLAUDE.md 함정 표 참고

---

## 5. 이번에 알아낸 것 — 다시 밟지 않도록

전부 `CLAUDE.md`의 **겪었던 함정** 표에 넣어 두었습니다. 그중 값이 컸던 셋:

**① 프롬프트 캐시 앞에 변하는 내용을 두면 안 된다**
직전 문맥(문장마다 바뀜)을 설교 원고 블록 *앞*에 붙였더니 캐시가 한 번도
적중하지 않아 한 시간 예배에 **$40**이 나갔습니다. `로그.txt`의 `캐시읽기`가
계속 0이면 이 상태입니다.

**② 구글 STT는 한 응답에 두 가지를 보낸다**
`results[0]` = 지금까지의 전체 문장(안정도 0.9), `results[1]` = 아직 흔들리는
뒷조각(0.01). 섞어 쓰면 확정 구간이 늘 0이 되어 자막이 아예 안 나갑니다.

**③ 규칙의 값어치는 엔진에 따라 다르다**
"연속 두 번 일치해야 확정"(LocalAgreement)은 V1에서는 0.25초짜리 공짜 보험이지만
chirp_3에서는 12초 지연이 됩니다. 그래서 중간 결과 간격이 1.5초를 넘으면
그 규칙을 건너뛰도록 해 두었습니다.

---

## 6. 고칠 때의 순서

CLAUDE.md의 작업 원칙을 그대로 따르되, 검증은 이 순서로 하십시오.

1. **양쪽 파일 모두 고쳤는지 확인** — `배포빌드/server.py`와 루트 `server.py`
2. `python 개발도구/문장끊기_테스트.py` — 비용 0, 3초
3. 캐시 쪽을 건드렸다면 `python 개발도구/캐시_적중확인.py`
4. 빌드 → 설치 → **실제 실행** (CLAUDE.md 빌드 절차)
5. 실제 설교 음성으로 `python 개발도구/자막_시간측정.py`

각 도구의 사용법은 [개발도구/README.md](개발도구/README.md)에 있습니다.
