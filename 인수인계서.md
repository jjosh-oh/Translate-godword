# LiveWord 인수인계서

새로 맡으시는 분께. **이 문서 하나만 따라가면 예배에 쓸 수 있습니다.**

- 프로그램: LiveWord — 설교를 실시간으로 인식·번역해 송출 화면과 교인 휴대폰에 자막·음성으로 전달
- 저장소: `jjosh-oh/Translate-godword`
- 현재 버전: **1.1**
- 문서 기준일: 2026-09-06

> **먼저 읽어야 할 것**
> - 운영만 하실 분 → 이 문서의 **1 ~ 5장**
> - 개발까지 이어받으실 분 → 이 문서 전체 + [`CLAUDE.md`](CLAUDE.md)

---

## 0. 가장 먼저 — 30분 안에 끝내는 순서

새 계정으로 넘어오면 이 순서대로 하십시오. 자세한 내용은 각 장에 있습니다.

| | 할 일 | 걸리는 시간 | 이 문서 |
|---|---|---|---|
| 1 | **Anthropic 계정 만들고 결제수단 등록 + 사용 한도(Budget) 걸기** | 10분 | [2-1](#2-1-anthropic-번역) |
| 2 | **Google Cloud 프로젝트 만들고 STT·TTS 켜고 서비스 계정 JSON 받기** | 15분 | [2-2](#2-2-google-cloud-음성-인식과-번역-음성) |
| 3 | **ngrok 계정 만들고 authtoken + 고정 도메인 받기** | 5분 | [2-3](#2-3-ngrok-교인-휴대폰-외부-접속) |
| 4 | 프로그램 설치하고 설정 화면에서 위 3개 입력 | 5분 | [3](#3-설치와-첫-실행) |
| 5 | 마이크 연결해서 시험 송출 | 10분 | [4](#4-예배-당일-운영-순서) |
| 6 | 고정 도메인이 바뀌었으면 **QR 포스터 다시 인쇄** | 5분 | [2-3](#2-3-ngrok-교인-휴대폰-외부-접속) |

> ⚠️ **1번의 "사용 한도 걸기"를 건너뛰지 마십시오.** 2026년 9월 6일 주일에 설정 하나가 잘못돼
> 한 시간 예배에 **$40**이 나간 일이 있습니다. 원인은 고쳤지만, 한도는 반드시 걸어 두십시오.

---

## 1. 무엇을 넘겨받는가

프로그램 자체는 무료지만, **바깥의 유료 서비스 세 곳**에 의존합니다. 계정을 넘겨받는 게 아니라
**새 계정을 만들어 키만 갈아 끼우는 것**이 안전하고 간단합니다.

| 서비스 | 하는 일 | 없으면 |
|---|---|---|
| **Anthropic (Claude)** | 번역 | 자막이 아예 안 나옴 |
| **Google Cloud** | 음성 인식(STT) + 번역 음성(TTS) | 인식이 시작 안 되거나, 자막만 나오고 소리 없음 |
| **ngrok** | 교인 휴대폰의 외부 접속 | QR 접속 불가 (같은 와이파이에서만 가능) |
| GitHub | 코드 보관·업데이트 배포 | 운영만 할 거면 없어도 됨 |

세 서비스 모두 **쓴 만큼 과금**됩니다. 비영리단체 지원 프로그램(Goodstack 경유) 대상이 될 수
있으니 교회 명의로 신청해 보시길 권합니다.

---

## 2. 계정별로 할 일

### 2-1. Anthropic (번역)

1. <https://console.anthropic.com> 에서 계정을 만듭니다.
2. **Billing**에서 결제수단을 등록하고 크레딧을 충전합니다.
3. ⭐ **Limits(사용 한도)에서 월 상한을 겁니다.** 처음에는 **월 $50** 정도를 권합니다.
   실제 사용량은 아래 [6장](#6-비용-얼마나-나오는가)에 실측치가 있습니다.
4. **API Keys**에서 키를 하나 만들어 복사합니다. (`sk-ant-...`로 시작)

> 키는 만들 때 한 번만 보입니다. 안전한 곳에 보관하시고,
> **채팅창·문서·git에 절대 붙여넣지 마십시오.**

**알아 두실 것**: "번역이 갑자기 멈췄다"는 증상은 대개 프로그램 고장이 아니라
**크레딧 소진**입니다. 잔액부터 확인하십시오.

### 2-2. Google Cloud (음성 인식과 번역 음성)

1. <https://console.cloud.google.com> 에서 계정을 만들고 **새 프로젝트**를 만듭니다.
2. 결제 계정을 연결합니다. (신규 가입 무료 크레딧이 있습니다)
3. **API 및 서비스 → 라이브러리**에서 아래 **두 개를 모두** 사용 설정합니다.
   - `Cloud Speech-to-Text API` — 설교 음성 인식
   - `Cloud Text-to-Speech API` — 번역 음성 합성
4. **IAM 및 관리자 → 서비스 계정**에서 서비스 계정을 만듭니다.
   - 역할은 `Cloud Speech 클라이언트` 정도면 충분합니다.
5. 그 서비스 계정의 **키 → 새 키 만들기 → JSON**으로 파일을 내려받습니다.
6. 받은 파일을 `google-key.json`으로 이름을 바꿔 안전한 곳에 둡니다.

> 이 JSON 파일이 곧 비밀번호입니다. git에 올라가지 않도록 `.gitignore`에 이미 막아 두었습니다.

### 2-3. ngrok (교인 휴대폰 외부 접속)

1. <https://dashboard.ngrok.com> 에서 계정을 만듭니다.
2. **Your Authtoken**을 복사합니다.
3. **Domains**에서 **고정 도메인**을 하나 만듭니다. (무료 플랜도 1개 가능)
   - 예: `이름-이름-이름.ngrok-free.dev`

> ⚠️ **고정 도메인이 바뀌면 교인용 QR 코드와 안내 포스터가 전부 무효가 됩니다.**
> 새 도메인을 받았다면 프로그램의 `/poster` 화면에서 포스터를 다시 뽑아 인쇄하십시오.
>
> ⚠️ 무료 플랜은 **동시 접속 약 10명** 제한이 있습니다. 그보다 많은 교인이 쓰실 거면
> 유료 플랜이 필요합니다.

### 2-4. GitHub (개발을 이어받을 때만)

현재 저장소 주인은 `jjosh-oh`입니다. 이어받는 방법은 두 가지입니다.

- **소유권 이전**: 저장소 Settings → Transfer ownership (기존 주인이 실행)
- **포크**: 새 계정에서 Fork 해서 독립적으로 관리

> **알려진 함정**: 이 PC에는 GitHub 계정이 둘 얽혀 있습니다. 저장소 주인은 `jjosh-oh`인데
> `gh` CLI는 읽기 전용 계정(`jjoshoh`)으로 로그인돼 있어서 `gh release create`가 404로 실패합니다.
> 새 계정에서는 `gh auth login`을 다시 하십시오.

---

## 3. 설치와 첫 실행

### 3-1. 설치

[최신 릴리스](https://github.com/jjosh-oh/Translate-godword/releases/latest)에서
`LiveWord_Setup.exe`가 든 ZIP을 내려받습니다.

> **ZIP 비밀번호는 릴리스 설명에 적혀 있지 않습니다.** 배포자에게 따로 받으십시오.
>
> 압축을 풀 때 `0x80004005` 오류가 나면 **7-Zip**으로 푸십시오.
> (윈도우 기본 압축 해제는 이 방식의 암호를 못 풉니다.)

### 3-2. 첫 실행 — 키 입력

프로그램을 처음 실행하면 **설정 화면**이 열립니다. 여기에 2장에서 받은 것을 넣습니다.

| 칸 | 넣을 것 |
|---|---|
| Anthropic API Key | `sk-ant-...` |
| Google 서비스 계정 JSON | 파일을 선택하면 자동 복사됩니다 |
| ngrok Authtoken | 대시보드에서 복사한 값 |
| ngrok 고정 도메인 | `이름-이름-이름.ngrok-free.dev` |

입력하면 `.env` 파일이 자동으로 만들어집니다. 직접 편집할 일은 거의 없지만, 형식은
[`.env.example`](.env.example)에 설명해 두었습니다.

**저장 위치**: `.exe`가 있는 폴더를 먼저 쓰고, 쓰기가 막혀 있으면 `%APPDATA%\LiveWord`로 넘어갑니다.

```
C:\Users\<사용자>\AppData\Local\Programs\LiveWord\
```

### 3-3. 잘 됐는지 확인

브라우저에서 `http://127.0.0.1:5000/settings` 를 열어 JSON이 보이면 정상입니다.

---

## 4. 예배 당일 운영 순서

예배 **10분 전**에 시작하십시오.

1. **LiveWord 실행**
2. 브라우저에서 **`http://localhost:5000/operator`** (운영자 화면)
3. **마이크 선택** — 설교자 마이크가 들어오는 입력 장치를 고릅니다
4. **언어 설정** — 왼쪽이 설교 언어(원어), 오른쪽이 송출 화면에 나갈 언어
   > ⚠️ **프로그램을 껐다 켜면 이 설정이 초기값(한국어 → 영어)으로 돌아갑니다.**
   > 매번 다시 확인하십시오. **음성(TTS) 토글도 꺼진 상태로 돌아갑니다.**
5. **송출 창 열기** → 스크린이나 OBS 브라우저 소스에 연결
6. **마이크 시작** → 운영자 화면 미리보기에 원문·번역이 흐르는지 확인
7. 교인 안내 — QR 코드는 `/guide` 또는 `/poster` 화면
8. 예배가 끝나면 **마이크 정지 → 송출 종료**

**설교 원고가 있으면** 운영자 화면에서 올리십시오. 고유명사(사람 이름, 지명, 성경 인명) 표기가
일관되게 나옵니다. `.txt` `.pdf` `.docx`를 지원합니다.

---

## 5. 문제가 생기면

증상별로 **확인 순서**입니다. 위에서부터 차례로 보십시오.

| 증상 | 먼저 볼 것 |
|---|---|
| **번역이 갑자기 멈춤** | Anthropic **크레딧 잔액**. 대개 이것입니다 |
| **자막이 아예 안 나옴** | 운영자 화면에서 마이크가 "시작" 상태인지 |
| **소리가 안 남** | 운영자 화면의 **음성 토글**. 재시작하면 꺼져 있습니다 |
| **아이폰에서만 소리가 없음** | 무음 스위치. 교인이 휴대폰 화면의 **🔊 버튼**을 눌러야 잠금이 풀립니다 |
| **QR이 안 뜸 / 송출 창이 안 열림** | 죽지 않은 `LiveWord.exe`·`ngrok.exe`가 포트 5000을 잡고 있음. 둘 다 종료 후 재실행 |
| **번역이 엉터리** | 대개 번역이 아니라 **음성 인식** 문제입니다. `로그.txt`에서 *입력*이 뭘로 찍혔는지 먼저 보십시오 (예: "Pardon me" → "Garden me") |
| **휴대폰에 옛날 화면이 남음** | 새로고침. 그래도 남으면 브라우저 캐시 삭제 |
| **요금이 이상하게 많이 나옴** | `로그.txt`의 `토큰:` 줄에서 **캐시읽기가 0인지** 확인. [6장](#6-비용-얼마나-나오는가) 참고 |

### 진단 1순위 자료 — `로그.txt`

설치 폴더에 있습니다. 번역할 때마다 이렇게 남습니다.

```
[16:03:18] 입력: 우리가 성경을 읽을 때 많은 씨앗과 만나게 됩니다.
[16:03:18] 번역: When we read the Bible, we encounter many seeds.
[16:03:18] 토큰: 입력 134 · 캐시읽기 5523 · 캐시기록 0 · 출력 19 | 이번 $0.0039 · 누적 $0.04
```

- **입력** 줄이 이상하면 → 음성 인식 문제 (마이크 위치, 잡음)
- **번역** 줄만 이상하면 → 번역 문제 (용어집·대응표로 개선)
- **누적 $** → 프로그램을 켠 뒤 지금까지 쓴 돈. 폰에서 고른 언어까지 전부 합산됩니다

---

## 6. 비용 — 얼마나 나오는가

실제 주일예배(2026-09-06, 08:49~12:10) 로그로 잰 값입니다.

- 설교 3시간 20분 = **번역 호출 864회**
- 설교 원고를 올린 상태에서 **대표 언어 하나 = 약 $3.5**
- **교인 휴대폰에서 고른 언어 하나마다 같은 금액이 더 붙습니다**
  (아무도 안 고른 언어는 번역이 아예 안 돌아서 비용 0)

즉 대표 언어 + 폰에서 2개 언어를 더 쓰면 예배당 **약 $10** 선입니다.

**꼭 기억하실 것**: 예전에 한 시간에 $40이 나온 적이 있습니다. 원인은 설교 원고가
매 문장마다 새로 청구되던 설정 문제였고 지금은 고쳤습니다. 다시 생기면
`로그.txt`의 **캐시읽기가 계속 0**으로 찍히고, 10회 연속이면 경고 줄이 자동으로 남습니다.

Google STT·TTS 요금은 이보다 훨씬 적지만, Google Cloud 콘솔에서도 **예산 알림**을 걸어 두십시오.

---

## 7. 지워지면 안 되는 파일

설치 폴더(`...\Programs\LiveWord\`)에 있습니다. **업데이트해도 유지되지만, 백업해 두십시오.**

| 파일 | 내용 |
|---|---|
| `.env` | API 키·토큰 — **가장 중요** |
| `google-key.json` | Google 인증 파일 |
| `mapping.txt` | 이름·용어 대응표 (예: `요한=John`) |
| `glossary.txt` | 음성인식 힌트용 교회 용어 278개 |
| `로그.txt` | 번역 기록 — 문제 진단 1순위 |

---

## 8. 개발을 이어받으실 분께

### 8-1. 먼저 읽을 것

**[`CLAUDE.md`](CLAUDE.md)** — 작업 원칙, 구조, 그동안 실제로 시간을 많이 쓴 함정들이
정리돼 있습니다. Claude Code로 작업하시면 이 파일이 자동으로 읽힙니다.

### 8-2. 반드시 알아야 할 두 가지

**① 코드가 두 벌입니다.**

| 경로 | 역할 |
|---|---|
| `배포빌드\server.py` | **배포용 본체.** 실제 .exe에 들어감 |
| `server.py` | 개발·실험용 사본 |

자동으로 동기화되지 않습니다. **기능을 고칠 때는 양쪽 모두** 고쳐야 합니다.
한쪽만 고치면 "개발에선 되는데 배포하면 안 되는" 현상이 납니다. HTML도 마찬가지로 두 벌입니다.

**② 교회에서 매주 실제로 씁니다.** 예배 중 고장 나면 대안이 없습니다.
요청받은 범위 밖은 먼저 물어보고, 잘 돌아가는 부분은 그대로 두고, 새 작업은 브랜치에서 하십시오.
**빌드했으면 반드시 실행해서 확인**하십시오 — "빌드 성공"은 동작 확인이 아닙니다.

### 8-3. 개발 환경 준비

```bash
git clone https://github.com/jjosh-oh/Translate-godword.git C:\Claude
pip install -r 배포빌드/requirements.txt
pip install pyinstaller
```

추가로 필요한 것:
- **Python 3.13**
- **[Inno Setup 6](https://jrsoftware.org/isdl.php)** — 설치 프로그램 제작용
- **`배포빌드/ngrok.exe`** — [ngrok.com/download](https://ngrok.com/download)에서 받아 넣기 (약 31MB, git에 없음)
- **`google-key.json`**, **`.env`** — git에 없음. 2장 참고

개발 중 실행:

```bash
cd 배포빌드
python server.py
```

### 8-4. 빌드와 배포

```bash
# 1. 실행 중이면 먼저 종료 (파일이 잠겨 빌드가 실패합니다)
taskkill //F //IM LiveWord.exe; taskkill //F //IM ngrok.exe

# 2. exe 빌드
cd "C:/Claude/배포빌드" && python -m PyInstaller --noconfirm LiveWord.spec

# 3. 설치 프로그램 생성
"$LOCALAPPDATA/Programs/Inno Setup 6/ISCC.exe" LiveWord.iss

# 4. 설치
"C:/Claude/배포빌드/installer/LiveWord_Setup.exe" /VERYSILENT /SUPPRESSMSGBOXES /NORESTART

# 5. 확인 — 응답하면 정상
curl -s http://127.0.0.1:5000/settings
```

**새 버전을 낼 때는 버전을 두 곳 모두 올립니다.** 하나만 고치면 업데이트 알림이 어긋납니다.

- `배포빌드\server.py` → `APP_VERSION = "1.2"`
- `배포빌드\LiveWord.iss` → `#define AppVersion "1.2"`

배포 ZIP은 **ZipCrypto** 방식이어야 합니다. 윈도우 기본 압축 해제가 AES를 못 풀기 때문입니다.

### 8-5. 지금 브랜치 상태 (2026-09-06 기준)

`main`은 안정판이고, **`fix-subtitle-stall` 브랜치에 아직 배포하지 않은 수정 3건**이 있습니다.

| 커밋 | 내용 |
|---|---|
| `e55cf56` | 스페인어 등에서 자막이 60~80초씩 멈추던 문제 |
| `3d70f89` | **번역 비용 폭증 수정** — 설교 원고 캐시가 매번 무효화되던 문제 ($31 → $3.5) |
| `c0c4fc0` | 문장 끊기 개선 — 음성활동 이벤트 + LocalAgreement + 시간 안전망 |

세 건 모두 실제 빌드·설치 후 음성을 넣어 확인했고, **현재 이 PC에는 설치돼 있습니다.**
다만 **버전은 아직 1.1이고 릴리스는 하지 않았습니다.** 정식 배포하려면 8-4의 버전 올리기부터 하십시오.
